# TASK-030: Phase 4 – Agent Separation & LangGraph Workflow

## Overview

Phase 4 focuses on refactoring the backend architecture to separate concerns into distinct agents and orchestrating them using LangGraph. The main objectives are:

1. **Agent Separation**
   - Extract existing monolithic logic into dedicated agents:
     - `StoryAnalyst`
     - `ScenePlanner`
     - `Cinematographer`
     - `MoodDesigner`
     - `VisualPrompter`
     - `SFXPlanner`
     - `PanelComposer`
   - Each agent should expose a clear Pydantic `State` model and a single entry method.

2. **LangGraph Workflow**
   - Create `backend/app/workflows/enhanced_webtoon_workflow.py` implementing the new graph.
   - Define `EnhancedWebtoonState` with fields for story, panels, style, sfx, and final video path.
   - Implement node functions that call the respective agents and return updated state.
   - Wire edges with conditional routing (e.g., re‑plan if evaluation score < threshold).
   - Add a rewrite loop node that invokes agents for iterative improvement.

3. **Testing**
   - Add `backend/tests/test_enhanced_workflow.py` covering successful execution and a failure‑replan scenario.

4. **Documentation**
   - Update `docs/v2.0.0/TASK_TRACKER.md` to reflect completion of Phase 3 and start of Phase 4.
   - Add a new entry in the **Execution Order** section for Phase 4 tasks.

## Success Criteria

- All agents are independent, import‑able modules with unit tests (>80% coverage).
- The LangGraph workflow runs end‑to‑end producing a video file.
- CI passes with new tests.
- TASK_TRACKER.md shows Phase 4 as **In Progress**.

## Next Steps

- Implement agents (see `/backend/app/agents/` directory).
- Build the workflow graph.
- Write tests.
- Commit and push.

---

_Generated by Antigravity AI assistant._
